declare namespace API {
  type Catalog = {
    id: string;
    name: string;
    normalizedName: string;
  };

  type WorkItem = {
    workId: string;
    name: string;
    normalizedName: string;
  };
}
